#include <stdio.h>

int main()
{
    int x, y, result;
    char operator;

    printf("enter eq:");scanf("%d %c %d");

    switch(operator){
        case '+':
        result = x + y;
        break;
    }
    printf("Answer: %d", result);
}